# CMD Chat Environment Configuration
# Copy this file to .env and configure as needed

# Authentication tokens (comma-separated)
# Generate secure tokens with: openssl rand -hex 32
CMDCHAT_TOKENS=your-secret-token-1,your-secret-token-2

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
CMDCHAT_LOG_LEVEL=INFO
